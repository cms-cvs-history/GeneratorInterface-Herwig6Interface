process PROD = {

   # MessageLogger control
   #
   service = MessageLogger
   {
      untracked vstring destinations = {"cout"}
       
      # untracked vstring categories = { "FwkJob" }
 
      untracked PSet cout = 
      { 
         untracked PSet default = { untracked int32 limit = 0 }    # kill all messages in the log
         # untracked PSet FwkJob  = { untracked int32 limit = -1 }  # except *all* of FwkJob's
      }
       
      #untracked vstring fwkJobReports = {"FrameworkJobReport.xml"}
   }
    

   # control point for all seeds
   #
   service = RandomNumberGeneratorService
   {
      untracked uint32 sourceSeed = 98765
   }

   # physics event generation
   #
   include "GeneratorInterface/Herwig6Interface/data/HerwigHZZ4mu.cfi"
   replace Herwig6Source.maxEvents = 100
   replace Herwig6Source.herwigHepMCVerbosity = false
   replace Herwig6Source.herwigVerbosity = 1
   replace Herwig6Source.maxEventsToPrint = 1

    module GEN = PoolOutputModule { 
	untracked string fileName = "herwigHZZ4mu.root"
    }

    endpath e = { GEN }
}
